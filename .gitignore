# macOS metadata
.DS_Store

# Agent instructions (kept local, intentionally not shared)
AGENTS.md

# Swift Package Manager build output
.build/
.swiftpm/

# App bundle produced by build.sh
DeepSeekClock.app/
